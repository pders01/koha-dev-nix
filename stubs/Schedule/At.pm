# STUB: Schedule::At is not in nixpkgs
package Schedule::At;
use strict;
use warnings;
use Exporter 'import';

our $VERSION = '999.0';
our @EXPORT_OK = qw(add remove getJobs);

sub add { 1 }
sub remove { 1 }
sub getJobs { () }

1;
